let arrObj = []
const emp = [[1, 5, [2, 3]], [2, 3, []], [3, 3, []]]

emp.map((id, idx)=> {
  console.log(id)
  {id: id[idx][0], importance: id[idx][1]
  })

// for(let [id, importance, sub] of emp) {
//   forMap[id] = {importance, sub}
// }

// for (let i = 0; i < emp.length; i++) {
//   let [id, importance, sub] = emp[i]
//   forBasic[id] = {importance, sub}

// }

  console.log(arrObj)
